In order to run our code, follow these steps:

For Python:
1. Install Python 3
2. Paste the given code into a file, save it as anything ending in ".py"
3. Run the file by typing "python3 <filename>" in the terminal

For Haskell:
1. Install GHC
2. Paste the given code into a file, save it as anything ending in ".hs"
3. Run the file by typing "ghci <filename>" in the terminal